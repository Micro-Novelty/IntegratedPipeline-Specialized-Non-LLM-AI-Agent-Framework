[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "abstract_weights_core"
version = "0.7.5"
readme = "readme-docs/opt-README.md
requires-python = ">=3.9"
