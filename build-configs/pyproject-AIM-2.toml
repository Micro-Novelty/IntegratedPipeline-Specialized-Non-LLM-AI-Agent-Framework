[build-system]
requires = ["setuptools>=61", "wheel", "numpy>=2.1.0", "cython", "setuptools-rust"]
build-backend = "setuptools.build_meta"

[project]
name = "AbstractIntegratedModule-hybrid"
version = "0.7.7"
authors = [
    {name = "Micro-Novelty", email = "hernikpuspita5@gmail.com"},
]
description = "Integrated Pipeline with Specialized Non-LLM AI Agent Framework for hybrid x86_64 and aarch64 architecture."
readme = "readme-docs/README-AIM-2.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = ["numpy>=2.1.0"]

